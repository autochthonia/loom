import React from 'react'
import { shallow } from 'enzyme'

import CombatantList from './CombatantList'

describe('CombatantList', () => {
  // let component, props
  //
  // beforeEach(() => {
  //   props = {}
  //   component = shallow(<CombatantList {...props} />)
  // })
  //
  // it('should', () => {
  //   expect(component).toMatchSnapshot()
  // })
})
